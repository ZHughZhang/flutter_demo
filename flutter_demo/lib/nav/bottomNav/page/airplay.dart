import 'package:flutter/material.dart';


class airPlayPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
	    appBar: AppBar(
		    title: Text('AirPlayPage'),
	    ),
	    body: Center(
		    child: Text('AirPlay'),
	    ),
    );
  }
}
