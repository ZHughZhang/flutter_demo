import 'package:flutter/material.dart';


class emailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
	    appBar: AppBar(
		    title: Text('Emailpage'),
	    ),
	    body: Center(
		    child: Text('email'),
	    ),
    );
  }
}
